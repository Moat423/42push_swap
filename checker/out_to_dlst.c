
#include "../push_swap.h"

int	main(int argc, char *argv[])
{
	//do ops.
	if (ft_sorted_descending(a->list, a->len) && b->len == 0)
		ft_printf("OK\n");
	//error on: arg not int, duplicates
	//std in invalid instruction, invalid format
	ft_printf("Error\n");
	return (1);
}
